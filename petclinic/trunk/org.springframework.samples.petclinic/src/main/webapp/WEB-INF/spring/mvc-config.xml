<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd">

	<!-- HANDLER MAPPING RULES -->
	
	<!-- Maps requests to @Controllers based on @RequestMapping("path") annotation values
		 If no annotation-based path mapping is found, Spring MVC sends a 404 response and logs a pageNotFound warning. -->
	<bean class="org.springframework.web.servlet.mvc.annotation.DefaultAnnotationHandlerMapping">
		<property name="order" value="1" />
	</bean>
	
	<!-- REGISTERED HANDLER TYPES -->

	<!-- Enables annotated @Controllers; responsible for invoking an annotated POJO @Controller when one is mapped. -->
	<bean class="org.springframework.web.servlet.mvc.annotation.AnnotationMethodHandlerAdapter">
		<!-- Configures Spring MVC DataBinder instances globally -->
		<property name="webBindingInitializer">
			<bean class="org.springframework.web.bind.support.ConfigurableWebBindingInitializer">
				<property name="formatterRegistry" ref="formatterRegistry" />
	        </bean>
		</property>
	</bean>

	<!--  VIEW RESOLUTION AND RENDERING -->
	
	<!-- Resolves view names to tiles definitions in /WEB-INF/tiles.xml -->
	<bean class="org.springframework.web.servlet.view.UrlBasedViewResolver">
		<property name="viewClass" value="org.springframework.web.servlet.view.tiles2.TilesView" />
	</bean>
		
	<!-- Initializes the Apache Tiles CompositeView system -->
	<bean id="tilesConfigurer" class="org.springframework.web.servlet.view.tiles2.TilesConfigurer">
		<property name="definitions">
			<list>
				<value>/WEB-INF/tiles.xml</value>
				<value>/WEB-INF/appointments/tiles.xml</value>
				<value>/WEB-INF/clients/tiles.xml</value>
			</list>
		</property>
	</bean>

	<!-- TYPE CONVERSION AND PROPERTY FORMATTING -->
	
	<!-- Enables the Spring 3 core.convert Type Coersion system -->
	<bean id="conversionService" class="org.springframework.core.convert.support.DefaultConversionService"/>
	
	<!-- Enables the Spring 3 ui.format Property Formatting system as a simpler alternative to PropertyEditors -->
	<bean id="formatterRegistry" class="org.springframework.samples.petclinic.util.PetclinicFormatterRegistry" />
	
</beans>