version=3.0.0
release.type=integration
project.key=TEMPLATES/MVC
ivy.cache.dir=${basedir}/../../../plugin-build/ivy-cache
integration.repo.dir=${basedir}/../../../plugin-build/integration-repo
source.version=1.5

# For when releasing
#release.type=release
#build.stamp=RELEASE

# For when releasing milestones
#release.type=milestone
#build.stamp=M1

# For development in trunk
#release.type=integration


